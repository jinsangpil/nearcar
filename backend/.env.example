# 니어카 백엔드 환경 변수 예시 파일
# 실제 사용 시 .env 파일로 복사하여 사용하세요

# 데이터베이스 연결 정보
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nearcar_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# 암호화 키 (AES-256, 32바이트)
# 생성 방법: openssl rand -base64 32
ENCRYPTION_KEY=your_32_byte_encryption_key_here

# JWT 시크릿 키
# 생성 방법: openssl rand -base64 64
JWT_SECRET_KEY=your_jwt_secret_key_here

# Redis 연결 정보 (선택적)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# AWS S3 설정 (선택적)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=ap-northeast-2
AWS_S3_BUCKET=nearcar-uploads

# 외부 API 키 (선택적)
MOLIT_API_KEY=your_molit_api_key
CAR365_API_KEY=your_car365_api_key

# 알림 서비스 API 키 (선택적)
KAKAO_ALIMTALK_API_KEY=your_kakao_alimtalk_key
NHN_SMS_API_KEY=your_nhn_sms_key
AWS_SES_REGION=ap-northeast-2

# Slack 웹훅 URL (선택적)
SLACK_WEBHOOK_URL=your_slack_webhook_url

# 환경 설정
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO

